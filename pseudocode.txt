1. Design the layout of the webpage (Header, Footer, etc.)
2. Create a sketch board
    1. Define a function to create a grid dynamically
    2. Size and style the sketch board
    3. Add a limit to the grid size
3. Add interactivity to the sketch board
    1. Add an event listener for simple drawing
    2. Add a button to reset the sketch board, but keeping the same size
    3. Add a button to reset the sketch board and change the size
    4. (OPTIONAL) Add buttons to change the pen color to specific colors
    5. (OPTIONAL) Add a button to change the pen color to random colors
    6. (OPTIONAL) Add a button for single tile erasing
    7. (OPTIONAL) Add a button to make the pen's brightness progressively change
4. Add final details
    1. Add error handling to #new-sketch button
    2. Add my github profile url